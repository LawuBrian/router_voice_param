# Azure Realtime API Configuration
# Rename this file to .env.local and fill in your values

# Azure Cognitive Services - Realtime Sessions URL
AZURE_REALTIME_SESSIONS_URL=https://your-resource.cognitiveservices.azure.com/openai/realtimeapi/sessions?api-version=2025-04-01-preview

# Azure WebRTC Endpoint
AZURE_REALTIME_WEBRTC_URL=https://eastus2.realtimeapi-preview.ai.azure.com/v1/realtimertc?model=gpt-realtime

# Azure API Key
AZURE_REALTIME_API_KEY=your-api-key-here

# Optional: Public Backend URL (for client-side configuration)
NEXT_PUBLIC_BACKEND_URL=https://your-backend-url.com

# ============================================
# Upstash Redis (Optional - for session persistence)
# Get free Redis at: https://upstash.com
# Without Redis, sessions are stored in-memory (lost on restart)
# ============================================
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here
